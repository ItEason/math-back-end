<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iteason.dao.TeacherDao">
    <select id="getAllPopular" resultType="com.iteason.entity.Teacher">
        select * from teacher order by total desc
    </select>

<!--    <update id="batchUpdate" parameterType="java.util.Map">-->
<!--        <foreach collection="teacherMap" item="item" index="index" separator=";">-->
<!--            update teacher set total = total + #{item.total} where id = #{item.id}-->
<!--        </foreach>-->
<!--    </update>-->
</mapper>